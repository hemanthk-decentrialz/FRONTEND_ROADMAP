{
  "name": "typescript-demo",
  "version": "1.0.0",
  "description": "TypeScript learning project",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "clean:win": "Remove-Item dist -Recurse -Force -ErrorAction SilentlyContinue"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}